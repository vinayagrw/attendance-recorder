# dependencies
node_modules/
.pnpm-store/
.pnp.*

# build outputs
dist/
build/
.vite/
*.tsbuildinfo

# env files (never commit secrets)
.env
.env.local
.env.*.local
apps/web/.env
apps/web/.env.local
apps/web/.env.*.local

# supabase local stack
supabase/.branches/
supabase/.temp/
supabase/.env

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# coverage + test runner outputs
coverage/
test-results/
playwright-report/
.playwright/
blob-report/

# claude / IDE / MCP / skills artefacts (kept out of source repo)
.claude/
.agents/
.junie/
.mcp.json
skills-lock.json
plan.md.draft
# we use pnpm only — guard against accidental npm/yarn lockfiles
package-lock.json
yarn.lock
