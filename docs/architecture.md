# Architecture

This is a placeholder. The authoritative architecture document for the MVP is the plan file at `.claude/plans/i-need-to-build-binary-giraffe.md`.

Once we ship M2, copy the relevant sections (data flow, schema, RLS) here so the repo is self-contained.
